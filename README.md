# trishita.in

Personal website of Trishita. Static site, hand-rolled with
[11ty](https://www.11ty.dev), deployed to GitHub Pages.

## Develop

```sh
npm install
npm run dev     # local server with live reload
npm run build   # output in _site/
```

Pushes to `develop` build and deploy automatically via GitHub Actions.
